type Query {
  room(id: ID!): Room!
}

type Mutation {
  createRoom: Room!
  createUser(name: String!): User!
}

type Subscription {
  userCreated: User
}

type Room {
  id: ID!
}

type User {
  name: String!
}
