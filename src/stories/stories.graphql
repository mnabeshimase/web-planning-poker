type Query {
  listStoriesByRoomId(id: ID!): [Story!]
}

type Mutation {
  createStory(roomId: String!, description: String!): Story!
}

type Subscription {
  storyCreated: Story!
}

type Story {
  id: ID!
  roomId: ID!
  description: String!
}
