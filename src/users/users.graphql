type Mutation {
  createUser(name: String!, roomId: ID!): User!
  deleteUser(id: ID!): User!
}

type Subscription {
  userCreated: User
  userDeleted: User
}

type User {
  id: ID!
  name: String!
  roomId: ID!
}
